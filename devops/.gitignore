# Secrets and environments — never commit real values
.env
.env.local
.env.production
.env.staging
*.log

# Allow the example/template file
!.env.example

# Ignore viva prep guide so it stays local and never gets pushed to GitHub
docs/viva_prep_guide.md
